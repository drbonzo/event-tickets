# Technical Requirements

- high traffic
    - caching
    - caching computed values, like available tickets counts, whether event has any tickets
- design just the API (for Frontend Application)
- SQL database

## Assumptions

- Frontend will use Authentication via JWT (or OAuth)
    - this will NOT be implemented
